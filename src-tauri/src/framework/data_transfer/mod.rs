//! 本地数据导出导入（sync-202609-001 L2）：数据包容器与 DTO
//!
//! 本批（C1）只落容器层：`.pbdata` 读写、清单结构与校验。命令层（§5.3 的 8 条冻结命令）、
//! 可导出目录、owner 适配器与隔离导入提交在 C2/C3 追加。
//!
//! 三条边界（与 L2 方案 §4.3 一致）：
//! - 秘密不进包：`secret` 策略的块不携带记录，导入后由用户重填
//! - 明文不进 JS：解密、解析、生成全在 Rust 侧
//! - 口令不落盘：口令只作为函数参数在单次调用内存活，不写设置文件、不进日志

#[allow(dead_code)] // C2/C3 命令层接入前，容器与 DTO 只有测试调用方
pub(crate) mod package;
#[allow(dead_code)] // 同上
pub(crate) mod types;
