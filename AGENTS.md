# CoveKit · 项目简报（AGENTS.md）

> 单人维护项目，agent 辅助开发；默认单 agent 串行完成，不套团队审批或角色流程。只保留不可遗漏约束，细节按任务读取。

**手册在仓库，不写进工具侧配置；常青规则保持工具中立。** 助手侧只放入口与环境适配，细则见[文档组织原则](docs/standards/17-文档组织与批次.md#仓库自有与工具中立)。

## 按请求进入

- 咨询、解释、评估、只读排查：按需读取并回答，默认不修改文件、不执行开发门禁、不提交。
- 明确的小改与既有行为修复：读取相关规范和实际调用链，实施、相关验证、提交；不强制任务文档。
- 有重要取舍或需跨会话接续时按[工作流](docs/standards/10-AI开发工作流.md)在本地维护一份方案；复杂工程才按需拆任务书，均不提交 Git。已有实施授权直接推进，未覆盖的重大范围、行为、兼容或安全取舍才澄清。
- [文档地图](docs/README.md)导航现行要求；本地 `TODO.md` 存在时按需查相关任务，缺失不阻塞开发。范围约束以[产品裁决](docs/standards/07-产品需求.md#当前有效范围裁决)为准，不依赖本地记录。
- 历史批次只作证据，不产生当前执行义务。用户当前指示及适用裁决优先；未裁决的实质冲突先给证据。写代码前核对实际调用链，不能把方案当成现状。

## 项目与稳定边界

- Tauri 2 + Rust，Vue 3 + TypeScript + Vite + Tailwind 4 + Pinia；Windows/macOS桌面工具箱，明净浅色、内容优先。技术选型见[01](docs/standards/01-技术选型.md)。
- 内置模块按业务owner分界；允许纯前端工具，不要求前后端目录一一对应。禁止跨owner内部import、直读对方状态或数据表；共享能力走公开契约。
- 框架公共能力在 `src/core/`、`src-tauri/src/framework/`；稳定兼容对象是用户数据与行为。删除功能须核对调用图、动态入口及启动/恢复职责。
- IPC经 `covekit_module!` 与 `covekit_routes!` 装配并入库；禁止手写第二套命令表。启动冲突fail-fast与调用契约测试缺一不可。
- 数据路径走 `framework::paths` 与唯一DataContext；PluginDb迁移只追加、事务执行，禁止将duplicate column整段视为成功。历史存储名允许显式映射。关闭/维护使用已有协调入口，单tab与应用退出清理范围不同。
- 凭据用系统keyring主密钥与AES-256-GCM文件，提供引用和手工输入双路径。失败不得生成新密钥冒充恢复成功；不在日志/文档/提交中记录秘密。
- 禁 `tauri-plugin-shell`。平台专用适配可用原生依赖和有SAFETY依据的unsafe；产品能力两端保持明确契约，unsupported可见。
- 详见[架构](docs/standards/02-架构.md)、[模块规则](docs/standards/03-模块开发规则.md)、[Rust规范](docs/standards/05-Rust代码规范.md)。现行说明与未完成任务有差异时核对活动方案和源码。

## UI与代码质量

- tokens以[DESIGN.md](DESIGN.md)为唯一来源；语义字号与字体，不新造色值。基础控件走 `@/core/ui`，凭证复合组件走 `@/core/vault`；基础Ui不依赖store/Vault/业务IPC。
- UI改动先读[11的必读摘要](docs/standards/11-插件UI开发约定.md#必读摘要)，然后只读相关专题。CodeMirror 6是唯一公共代码编辑器基座。
- 按状态归属、释放责任和修改原因决定拆分；不按行数、文件数机械重构，不为假想扩展新增抽象。本次范围外的问题记录必要线索。
- composable管理响应式状态/生命周期；纯函数单独命名。测试按[15](docs/standards/15-测试与走查.md)验证业务、故障、并发和资源责任；无逻辑透传由消费方覆盖，不为每文件补测试。可复用回归入库，一次性排查留 .work/。
- 每owner一个权威DTO定义域，可分文件；Rust与TS传输字段同步。同步锁不跨await；clone按成本和所有权评审。
- 操作结果可感知；禁止静默catch。取消/预期缺省不是错误，IO/解析/密钥失败不得伪装成功。注释描述公共契约、安全和资源不变量。
- 验证命令唯一维护在[20-验证矩阵](docs/standards/20-验证矩阵.md)。文档门禁不编译Rust；不得把未验证写成已通过。

## 工作区与提交

- 文档与注释中文，代码标识符英文。仓库只维护现行要求、架构、契约和必要决策；任务书、方案、进度与人工待办只留本地，默认放被忽略的 .work/。旧 docs/batches/、TODO.md 和人工验证清单保留本地，不重新跟踪。
- 先 `git status`，只改自己负责文件；遇到用户或其它会话的改动先核对归属。提交前检查整个暂存区；只 `git add <明确路径>`，禁 `git add .` / `git add -A`，不卷走不属于本任务的暂存。
- 本地只格式化本次文件；CI只读检查。复杂工作记录起始HEAD，不造空基线提交。不为消除工作区差异而stash、reset或覆盖已有改动。
- 对已授权实施的改动默认自动commit、只commit不push；咨询不制造提交。提交标题要自包含，读标题即知改了什么：禁批次号、工作项号与评审编号（如 `L2`、`C1`、`D3`、`M15`），禁括号与破折号补充说明，禁笼统过程描述；正文与细则见[21](docs/standards/21-Git提交规范.md)，`.githooks/commit-msg` 在提交时本地拦截。
- 每会话首次提交前明确用户名+邮箱；已有授权可沿历史提交核对，无local配置再设置local，不修改global。
- 代码与受影响的现行说明可同提交；不提交过程记录、进度回填或收尾报告。长期有效裁决提炼到规范，不要求读本地方案才能理解仓库。
- 不按进程名或端口强杀应用；只清理由本任务启动并确认归属的进程。用户实例走正常关闭，未知实例先核对归属。

## Windows环境

所有命令用Git Bash。pnpm shim异常时：
`export PATH="/c/Users/patchy/AppData/Roaming/npm:$PATH"`。
进程/平台工具按当前环境选择，不假定某个助手专有API存在。
