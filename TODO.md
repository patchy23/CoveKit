# patchyBox · 待办总表（跨会话）

> **进度不在这里看**——每份任务书执行到哪、哪些只有任务书还没执行，见 [`docs/进度台账.md`](docs/进度台账.md)（唯一权威视图）。
> 本文只记待办事项、用户裁决（明确不做 / 已搁置）。

> 由AGENTS指向，每次开工必读本表。这里只记当前范围和裁决；完成进度在台账更新，本文只在范围或裁决改变时调整。

---

> **分工**：本文只记**进行中的待办**与**用户裁决**（明确不做 / 已搁置）。
> 每份任务书执行到哪、哪些还没开工，看 [`docs/进度台账.md`](docs/进度台账.md)（进度唯一权威视图）；
> 已完成的任务不再在本文留档（需要细节时查 git 历史）。

## 五、待办 · 框架可靠性与扩展治理

> 定案见 [`剩余项决策书.md`](docs/batches/arch-202609-001-架构重构与规范调整/剩余项决策书.md)；
> 各任务书的执行状态看 [`docs/进度台账.md`](docs/进度台账.md)。本节只留需要拍板或尚未开工的事。

任务书：`docs/batches/rel-202609-001-可靠性与扩展治理/可靠性与扩展治理任务书.md`（13项必修；实施覆盖须逐验收项核对台账和源码，不以提交关键词未命中推断零实现）。

- [x] A 批：T04 / T05，系统密钥库真实接入、旧密钥兼容与凭证崩溃恢复。证据与未验证项见 [A批交接与验收证据](docs/batches/rel-202609-001-可靠性与扩展治理/A批交接与验收证据.md)。
- [x] B 批：T01 / T02 / T03 / T06，存储生效根、离线迁移、旧布局恢复与事务迁移；证据见[B批交接与验收证据](docs/batches/rel-202609-001-可靠性与扩展治理/B批交接与验收证据.md)。
- [x] C 批：T07 / T08 / T09，设置一致性、双平台交付与凭证引用；证据见[C批交接与验收证据](docs/batches/rel-202609-001-可靠性与扩展治理/C批交接与验收证据.md)。
- [ ] D 批：T10 / T11 / T12 / T13，工作区生命周期、诊断、IPC 契约与结构治理。**T10、T11、T12、T13 已完成**（T13 的文档同步、架构守卫与 `vault/store.rs` 拆分已在第二轮收尾）；T10/T11 已于 2026-09-14 在正式版做真机走查并修复复制诊断的权限提示缺陷（`42d3f36`），页签快速开关计数、隐藏后连接恢复、退出时长任务三项因缺页内钩子、真实会话与长任务夹具未取证，见[D批交接与验收证据](docs/batches/rel-202609-001-可靠性与扩展治理/D批交接与验收证据.md)。

2026-09-14 用户裁决（框架行为口径，均维持现状）：非空目标目录不合并（只接受上次提交中断的续跑）、恢复页动作需重启后生效、设置保存失败即回滚不保留草稿；明文 secret 不另加源码级守卫（设置层运行期拒绝已覆盖）；组件实验室改为仅开发构建注册。

2026-09-14 真机走查（正式版 exe + CDP 驱动）修复了复制诊断信息触发 WebView2 剪贴板权限提示（改走原生剪贴板插件），提交 `42d3f36`；关闭主窗口最小化到托盘为设计行为。同批发现的快捷键「未生效」误报随该功能整体移除，不再存在。

2026-09-14 用户裁决（范围变更）：**全局唤起快捷键整功能移除**，理由是初期默认组合实际未被使用。插件依赖、设置字段与只读生效值、设置页入口、i18n 与规范文档一并去掉；需求本身不取消，日后需要时按 `07-产品需求.md` §14.3 重新引入（录制式输入 + 冲突检测 + 注册失败保留旧键）。实现提交 `95ea9d5`。移除后真机复验（正式版 exe + CDP 驱动）：应用启动不再占用 `Ctrl+Shift+Space`（外部 RegisterHotKey 抢注成功），`settings_get` 与 `settings.json` 均无该字段且其余设置未受影响，设置页无快捷键入口与未生效提示，正常退出走原有关闭流程。

- [ ] E 批（近期必做）：T15 / L0–L5，本地数据导入导出、隔离/合并与云同步数据接口预留。L1 开工时同步清理 L0 字段分类表里随快捷键移除而失效的 `globalHotkey` 行，不单独改动已冻结的边界表。任务书见 `docs/batches/sync-202609-001-导入导出与云同步/导入导出与云同步预留任务书.md`；L0 数据边界冻结已定稿（§13），L1 起进入实现，按 A–D 前置依赖推进。

用户 2026-09-12 确认：云账户为后期规划，用于多地登录同步配置、凭证、使用习惯；当前不做登录/云服务/实际同步，但必须预留稳定身份、空间与账户分离、传输适配和密钥边界。本期先实现本地数据功能。

P2 扩展 T14 / T16（工具意图、资源预算）仍仅为建议。后续执行前先核对任务书分析基线与当前代码，避免重复修复。

## 六、明确不做（本轮，用户 2026-09-11 定）

**SSH**：快速命令片段面板、`~/.ssh/config` 导入与配置导出、密钥生成与公钥部署、远程↔远程直传、跳板机 ProxyJump 多跳、多服务器同步输入（MultiExec）、日志自动轮转与压缩。

**编辑器**：语言服务级智能提示（LSP / 类型推断）、富文本与 Markdown 所见即所得、协同编辑、代码片段管理器。

**FRP**（v1 边界，2026-09-12 定）：关闭界面后 frpc 后台常驻与开机自启、流量与连接数监控、运行时热改代理（`/api/store/proxies`）、定时连接、frps 服务端管理、开 `webServer`。

---

## 七、已搁置（用户说过「先不管」，勿擅自开始）

| 事项                                             | 背景                                                                                     |
| ------------------------------------------------ | ---------------------------------------------------------------------------------------- |
| 下载默认目录 fallback 到 `C:/` 导致 `os error 5` | `FileManagerTab.vue` 的默认下载目录兜底；改法是换 Tauri `downloadDir()` + 把错误翻成人话 |
| 终端 ANSI 调色板 / 主题选择器                    | 用户 2026-09-11：现在颜色挺好，不需要                                                    |
| 远程终端 PS1 上色（改远端 bashrc）               | 同上，一并搁置                                                                           |
| 远程栏刷新按钮与本地栏对称                       | 曾问过用户，未表态                                                                       |
