# patchyBox · 待办总表（跨会话）

> **进度不在这里看**——每份任务书执行到哪、哪些只有任务书还没执行，见 [`docs/进度台账.md`](docs/进度台账.md)（唯一权威视图）。
> 本文只记待办事项、用户裁决（明确不做 / 已搁置）。

> 由AGENTS指向，每次开工必读本表。这里只记当前范围和裁决；完成进度在台账更新，本文只在范围或裁决改变时调整。

---

> **分工**：本文只记**进行中的待办**与**用户裁决**（明确不做 / 已搁置）。
> 每份任务书执行到哪、哪些还没开工，看 [`docs/进度台账.md`](docs/进度台账.md)（进度唯一权威视图）；
> 已完成的任务不再在本文留档（需要细节时查 git 历史）。

## 五、待办 · 框架可靠性与扩展治理

> 定案见 [`剩余项决策书.md`](docs/batches/arch-202609-001-架构重构与规范调整/剩余项决策书.md)；
> 各任务书的执行状态看 [`docs/进度台账.md`](docs/进度台账.md)。本节只留需要拍板或尚未开工的事。

任务书：`docs/batches/rel-202609-001-可靠性与扩展治理/可靠性与扩展治理任务书.md`（13项必修；实施覆盖须逐验收项核对台账和源码，不以提交关键词未命中推断零实现）。

- [x] A 批：T04 / T05，系统密钥库真实接入、旧密钥兼容与凭证崩溃恢复。证据与未验证项见 [A批交接与验收证据](docs/batches/rel-202609-001-可靠性与扩展治理/A批交接与验收证据.md)。
- [x] B 批：T01 / T02 / T03 / T06，存储生效根、离线迁移、旧布局恢复与事务迁移；证据见[B批交接与验收证据](docs/batches/rel-202609-001-可靠性与扩展治理/B批交接与验收证据.md)。
- [x] C 批：T07 / T08 / T09，设置一致性、双平台交付与凭证引用；证据见[C批交接与验收证据](docs/batches/rel-202609-001-可靠性与扩展治理/C批交接与验收证据.md)。
- [x] D 批：T10 / T11 / T12 / T13，工作区生命周期、诊断、IPC 契约与结构治理。**T10、T11、T12、T13 已完成**（T13 的文档同步、架构守卫与 `vault/store.rs` 拆分已在第二轮收尾）；T10/T11 已于 2026-09-14 在正式版做真机走查并修复复制诊断的权限提示缺陷（`42d3f36`），页签快速开关计数、隐藏再显示后连接仍正常、退出时任务在跑会被拒绝且可取消、错误清单在真实崩溃路径下的留档四项已于 2026-09-16 在开发构建补齐（「退出时长任务」按当日裁决改用「退出时有运行任务会被拒绝且可取消」口径）；真实更新安装按裁决标 ⏸（占位公钥与发布通道未就位），见[D批交接与验收证据](docs/batches/rel-202609-001-可靠性与扩展治理/D批交接与验收证据.md)。

2026-09-14 用户裁决（框架行为口径，均维持现状）：非空目标目录不合并（只接受上次提交中断的续跑）、恢复页动作需重启后生效、设置保存失败即回滚不保留草稿；明文 secret 不另加源码级守卫（设置层运行期拒绝已覆盖）；组件实验室改为仅开发构建注册。

2026-09-14 真机走查（正式版 exe + CDP 驱动）修复了复制诊断信息触发 WebView2 剪贴板权限提示（改走原生剪贴板插件），提交 `42d3f36`；关闭主窗口最小化到托盘为设计行为。同批发现的快捷键「未生效」误报随该功能整体移除，不再存在。

2026-09-14 用户裁决（范围变更）：**全局唤起快捷键整功能移除**，理由是初期默认组合实际未被使用。插件依赖、设置字段与只读生效值、设置页入口、i18n 与规范文档一并去掉；需求本身不取消，日后需要时按 `07-产品需求.md` §14.3 重新引入（录制式输入 + 冲突检测 + 注册失败保留旧键）。实现提交 `95ea9d5`。移除后真机复验（正式版 exe + CDP 驱动）：应用启动不再占用 `Ctrl+Shift+Space`（外部 RegisterHotKey 抢注成功），`settings_get` 与 `settings.json` 均无该字段且其余设置未受影响，设置页无快捷键入口与未生效提示，正常退出走原有关闭流程。
2026-09-14 用户裁决（工具级设置口径）：**工具级设置保留在设置页渲染，不下沉到工具页**，只做减法——凡不需要用户选择的项一律写死或交给应用自动处理，设置页工具区从 7 条收到 2 条。落地：格式转换 JSON 缩进固定 2 空格并删除设置项（4 空格与 Tab 不再可选）；SSH 意外断线自动重连改为始终开启；FRP 日志缓冲固定 2000 行；FRP 下载改为内置下载源自动回退（直连优先，失败依次走 `ghfast.top`、`ghproxy.net`、`gh.llkk.cc`，换源共享 5 分钟总时长），删除 `downloadMirror` 设置项；FRP 删除设置页的 `frpcPath` 输入项（该键仍由设置卡片的「选择本地 frpc」写入并被二进制探测优先读取，客户端管理弹窗「引用外部文件」为常驻入口）；顺带删除数据库插件从未生效的 `agentMirror` 死配置读取分支与镜像下载。保留 `ssh.idleDisconnectMinutes`（选错会打断正在做的事）与 `frp.profileDir`（配置需要能跟项目走）两项。实现提交 `44f03fd`。真机核对（正式版 exe + CDP 驱动）：设置页工具区只剩 SSH 与 FRP 两项；JSON 格式化输出 2 空格缩进；启动后 `settings.json` 中四个已移除键被清理、保留键与应用级设置未动；`frp_binary_versions` 正常返回，下载安装走通新下载循环（测试装的 0.70.1 客户端与登记行已还原）。当时 `data/frp/bin` 不存在 0.70.1 残留、`frp.db` 客户端行与验证前一致。

- [ ] E 批（近期必做）：T15 / L0–L5，本地数据导入导出、隔离/合并与云同步数据接口预留。L1 开工时同步清理 L0 字段分类表里随快捷键移除而失效的 `globalHotkey` 行，不单独改动已冻结的边界表（✅ 2026-09-15 随 L1 落地删除）；**L1 落地时必须把 `space_id()` 由 `&'static str` 放宽为动态值**（连带 `DataContext` 字段、`is_current` 与 `maintenance_guard` 的调用面），当前零成本语义会随之调整（✅ 2026-09-15 已落地，`DataContext.space_id` 已是 `String`）。任务书见 `docs/batches/sync-202609-001-导入导出与云同步/导入导出与云同步预留任务书.md`；L0 数据边界冻结已定稿（§13），L1 已实现（框架侧），L2–L5 未开工，按 A–D 前置依赖推进。

2026-09-15 用户裁决（范围变更）：L1 实现里 D8／D9 两处偏离——换盘迁移与校验枚举是否覆盖非默认空间、默认空间日志与缓存是否按空间分层搬移——**留到 L3 一起做**。L1 不补做（单空间下无可观察差异，且搬移会动既有会话日志）；两项并入 L3 范围，L3 开工时必须一并处理，不在 L3 之外单独补。证据见 `docs/batches/sync-202609-001-导入导出与云同步/L1实现方案.md` §6 与 §6.1。

2026-09-15 用户裁决（验证口径）：**本地不用 cargo 验证，改用 `pnpm run check`**。本地只做静态与格式（`pnpm run check`、`check_docs.py`、`check_rust_rules.py`），Rust 编译、测试、clippy、fmt 交 CI 或用户手动执行。由此产生的证据缺口必须如实声明，本地绿灯不得写成「已验证」。已落在 `docs/standards/20-验证矩阵.md` 的「本地与CI的分工」。

用户 2026-09-12 确认：云账户为后期规划，用于多地登录同步配置、凭证、使用习惯；当前不做登录/云服务/实际同步，但必须预留稳定身份、空间与账户分离、传输适配和密钥边界。本期先实现本地数据功能。

2026-09-14 用户裁决（范围与暂缓）：

- **全局唤起快捷键暂不做**：需求不取消，日后按 `07-产品需求.md` §14.3 重做（录制式输入 + 冲突检测 + 注册失败保留旧键）。
- **工具间数据传输（T14）不做**：等 T15 落地、出现第二个真实消费方再评估，不提前实现万能总线。
- **T16 只做测量部分**：把空闲启动、单 SSH、多工具切换、隐藏 10 分钟、反复开关页签的 CPU/内存/句柄/订阅数做成**公共的实时观测工具**（不是一次性脚本），**需先出实现方案，方案未定不开工**；与其配套的按 owner 缓存清理、无状态工具淘汰策略一并暂缓。
- **macOS 照常实现但不做平台实测**（无环境）：两端差异点集中登记在 [`standards/09-平台能力矩阵.md`](docs/standards/09-平台能力矩阵.md) §3，新增平台分支必须同步登记。
- **真实环境验证不阻塞收口**：真机取数类未验证项记录在案即可，不作为收口的完成条件。

P2 扩展 T14 / T16 仍仅为建议。后续执行前先核对任务书分析基线与当前代码，避免重复修复。

## 六、明确不做（本轮，用户 2026-09-11 定）

**SSH**：快速命令片段面板、`~/.ssh/config` 导入与配置导出、密钥生成与公钥部署、远程↔远程直传、跳板机 ProxyJump 多跳、多服务器同步输入（MultiExec）、日志自动轮转与压缩。

**编辑器**：语言服务级智能提示（LSP / 类型推断）、富文本与 Markdown 所见即所得、协同编辑、代码片段管理器。

**FRP**（v1 边界，2026-09-12 定）：关闭界面后 frpc 后台常驻与开机自启、流量与连接数监控、运行时热改代理（`/api/store/proxies`）、定时连接、frps 服务端管理、开 `webServer`。

---

## 七、已搁置（用户说过「先不管」，勿擅自开始）

| 事项                                             | 背景                                                                                     |
| ------------------------------------------------ | ---------------------------------------------------------------------------------------- |
| 下载默认目录 fallback 到 `C:/` 导致 `os error 5` | `FileManagerTab.vue` 的默认下载目录兜底；改法是换 Tauri `downloadDir()` + 把错误翻成人话 |
| 终端 ANSI 调色板 / 主题选择器                    | 用户 2026-09-11：现在颜色挺好，不需要                                                    |
| 远程终端 PS1 上色（改远端 bashrc）               | 同上，一并搁置                                                                           |
| 远程栏刷新按钮与本地栏对称                       | 曾问过用户，未表态                                                                       |
