# CoveKit v0.1.0 发布候选版

- 验证日期：2026-08-24
- 验证环境：Windows x64

## 产品范围

首个发布候选版集成了文本和网络轻工具、多驱动数据库工作台、三家云 DNS 管理、SSH 远程管理以及公共加密凭证库。核心应用外壳支持中英文切换，发布版可通过 GitHub Releases 检查、签名验证和安装更新。

## 自动化验证

- Prettier：通过。
- ESLint：通过，0 告警。
- Vue/TypeScript 生产构建：通过。
- Vitest：17 个测试文件、144 项测试通过。
- rustfmt：通过。
- clippy `--all-targets -D warnings`：通过。
- Rust 文档覆盖检查：58 个源文件通过。
- Windows NSIS 生产构建：通过。

## Windows 产物

- 文件：`CoveKit_0.1.0_x64-setup.exe`
- 大小：8,691,062 字节（约 8.29 MiB）
- SHA-256：`C5121DE1FE2ACB38A66F2585DA7283BC33D95F679150EC4C1597D5155051AA22`
- Authenticode：未签名，当前本机未提供 Windows 代码签名证书；发布前仍需在持有证书的 CI 环境复验。

## 发布前人工验证

- 在干净 Windows 设备安装、启动、卸载，检查 UAC、托盘和快捷键。
- 用真实 MySQL/PostgreSQL/Redis/SSH/DNS 账户执行连接与写操作回归。
- 配置 Tauri 更新密钥和平台代码签名证书，验证从旧版到候选版的完整更新链路。
- 在 macOS 完成 DMG、Developer ID 签名和 Apple 公证验证。

## 已知边界

- 当前英文包覆盖核心导航、顶栏和设置；部分工具内部业务文案仍为中文。
- 达梦数据库仅保留入口，未接入后端驱动。
- Cloudflare、真实 SSH 服务器和各商业数据库需由发布环境提供凭证才能完成在线验证。
