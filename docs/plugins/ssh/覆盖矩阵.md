# SSH 工具 · 需求覆盖矩阵

> 版本：v1.0（2026-08-09）  ·  每条需求 → 用例编号 → 状态
> 状态：`待执行`（依赖后端/联调） / `通过`（阶段 A 前端已验证） / `失败`（走查发现缺陷） / `不适用`（明确不做）

## P0 必须（MVP 核心）

### P0-1 服务器连接管理

| 用例 | 描述 | 状态 | 备注 |
|------|------|------|------|
| SSH-C-01 | 侧栏展示已保存服务器列表 | 通过 | mock 3 台，状态点/名称/状态文本渲染正确 |
| SSH-C-02 | 添加服务器（表单 + 保存 + toast） | 通过 | 无必填校验，见 UI-014 |
| SSH-C-03 | 编辑服务器（回填 + 保存） | 通过 | 密码/密钥不回显（设计如此） |
| SSH-C-04 | 删除服务器（确认弹窗 + 删除） | 通过 | 确认弹窗实现正确 |
| SSH-C-05 | 连接：未连接→连接中→已连接 | 通过 | mock 800ms 模拟；真实认证待后端 |
| SSH-C-06 | 断开连接（状态复位 + toast） | 通过 | |
| SSH-C-07 | 搜索过滤（名称/host/用户名） | 通过 | |
| SSH-C-08 | 单击选中 / 切换服务器 | 通过 | |
| SSH-C-09 | 列表空态「暂无服务器」 | 通过 | |

### P0-2 SSH 终端

| 用例 | 描述 | 状态 | 备注 |
|------|------|------|------|
| SSH-T-01 | 终端区域随连接状态显示（已连接假输出 / 未连接提示） | 通过 | 假数据渲染 |
| SSH-T-02 | 键盘输入 → IPC 写入（ssh_terminal_write） | 待执行 | 后端 + xterm.js |
| SSH-T-03 | 输出推送 → xterm.js 渲染（ANSI） | 待执行 | `ssh://terminal-data` |
| SSH-T-04 | 窗口 resize → window-change | 待执行 | ssh_terminal_resize |
| SSH-T-05 | Ctrl+C/D/Z 控制字符透传 | 待执行 | |
| SSH-T-06 | 滚动缓冲 | 待执行 | xterm.js |
| SSH-T-07 | 终端状态栏（行列/编码/延迟） | 失败 | 延迟未显示，见 UI-005 |
| SSH-T-08 | 清空终端 | 失败 | 按钮无事件绑定，见 UI-003 |
| SSH-T-09 | 重新连接 | 失败 | 按钮无事件绑定，见 UI-003 |
| SSH-T-10 | 右键菜单（复制/粘贴/清空/重连） | 失败 | 未实现，见 UI-004 |

### P0-3 文件管理（查看/上传/下载）

| 用例 | 描述 | 状态 | 备注 |
|------|------|------|------|
| SSH-F-01 | 远程目录列表（名称/大小/修改时间） | 通过 | 额外展示权限/所有者 |
| SSH-F-02 | 路径导航（输入回车跳转 / 上级按钮） | 通过 | 面包屑缺失，见 UI-015 |
| SSH-F-03 | 双击目录进入 | 通过 | |
| SSH-F-04 | 上传（Tauri 对话框 + 进度） | 待执行 | 当前 toast 占位（UI-019） |
| SSH-F-05 | 下载（保存对话框 + 完成 toast） | 待执行 | 当前 toast 占位（UI-019） |
| SSH-F-06 | 删除（确认弹窗 → 刷新） | 失败 | 无确认弹窗，见 UI-009 |
| SSH-F-07 | 选中文件 + 「已选」提示 | 通过 | |
| SSH-F-08 | 重命名 | 待执行 | 当前 toast 占位（UI-019） |
| SSH-F-09 | 断线时文件操作反馈 | 待执行 | 后端 |

### P0-4 凭证管理（密码/密钥）

| 用例 | 描述 | 状态 | 备注 |
|------|------|------|------|
| SSH-CR-01 | 认证方式三选（密码/私钥/私钥+passphrase） | 通过 | |
| SSH-CR-02 | 表单字段联动显示（密码框/私钥框/passphrase） | 通过 | |
| SSH-CR-03 | 禁止明文落盘（secretRef + stronghold） | 待执行 | 后端，联调验证 |
| SSH-CR-04 | 编辑回显不回显密码/密钥 | 通过 | 设计如此（不落盘） |
| SSH-CR-05 | 私钥内容多行输入 | 通过 | textarea |

## P1 建议（完整体验）

### P1-5 远程编辑

| 用例 | 描述 | 状态 | 备注 |
|------|------|------|------|
| SSH-E-01 | 双击文本文件打开编辑器 | 通过 | 弹窗式（非页签，见 UI-017） |
| SSH-E-02 | CodeMirror 编辑（行号/语法高亮/脏标记） | 通过 | json/xml 高亮，dirty 徽标 |
| SSH-E-03 | 保存回写 + toast | 通过 | mock 保存；真实上传待后端 |
| SSH-E-04 | 未保存关闭提示「是否保存？」 | 失败 | 直接关闭无提示，见 UI-013 |
| SSH-E-05 | 编码自动检测（UTF-8/GBK） | 待执行 | 后端 |

### P1-6 资源监控

| 用例 | 描述 | 状态 | 备注 |
|------|------|------|------|
| SSH-M-01 | CPU/内存/磁盘/网络四卡片 | 通过 | |
| SSH-M-02 | 默认 3s 轮询 | 通过 | 随机波动假数据 |
| SSH-M-03 | 刷新间隔切换（1s/5s/10s/手动） | 失败 | 未实现，见 UI-011 |
| SSH-M-04 | 迷你折线图（最近 60 点） | 通过 | 上限 60 点 |
| SSH-M-05 | 断线提示「连接已断开，停止刷新」 | 失败 | 未实现，见 UI-012 |
| SSH-M-06 | 最后更新时间展示 | 通过 | |

### P1-7 服务管理

| 用例 | 描述 | 状态 | 备注 |
|------|------|------|------|
| SSH-S-01 | systemd 服务列表 + 状态 | 通过 | |
| SSH-S-02 | 状态筛选（全部/运行中/已停止/失败） | 通过 | |
| SSH-S-03 | 启动/停止/重启操作 | 待执行 | 当前 toast 占位（UI-019） |
| SSH-S-04 | 停止/重启二次确认 | 失败 | 未实现，见 UI-010 |
| SSH-S-05 | 日志弹窗（journalctl -u -n 50） | 待执行 | 当前 toast 占位（UI-019） |
| SSH-S-06 | 服务计数 | 通过 | |

### P1-8 进程管理

| 用例 | 描述 | 状态 | 备注 |
|------|------|------|------|
| SSH-P-01 | 进程列表（PID/用户/CPU%/MEM%/命令） | 通过 | |
| SSH-P-02 | 排序（CPU/内存/PID） | 通过 | 下拉选择（非表头点击，见 UI-018） |
| SSH-P-03 | 搜索（名称/PID/用户） | 通过 | |
| SSH-P-04 | 详情弹窗（ps -fp） | 失败 | 未实现，见 UI-007 |
| SSH-P-05 | 结束进程确认（kill/kill -9） | 失败 | 无确认弹窗，见 UI-008 |
| SSH-P-06 | 刷新列表 | 失败 | 按钮无事件绑定，见 UI-006 |

## P2 可延后

### P2-9 Docker 容器

| 用例 | 描述 | 状态 | 备注 |
|------|------|------|------|
| SSH-D-01 | 容器列表（名称/镜像/状态/端口） | 通过 | |
| SSH-D-02 | 启动/停止/重启/删除 | 待执行 | 当前 toast 占位（UI-019） |
| SSH-D-03 | 日志弹窗（--tail 100） | 待执行 | 当前 toast 占位（UI-019） |
| SSH-D-04 | exec 进入终端（新终端页签） | 待执行 | 当前 toast 占位（UI-019） |
| SSH-D-05 | 搜索/状态筛选 | 通过 | |
| SSH-D-06 | 刷新 | 失败 | 按钮无事件绑定，见 UI-006 |

### P2-10/11/12 终端分屏 / 快速命令 / 主题自定义

| 用例 | 描述 | 状态 | 备注 |
|------|------|------|------|
| SSH-X-01 | 无越界实现（三者均无 UI/契约入口） | 通过 | 契约无对应命令，UI 无入口 |

## 边界回归（需求 §4 不做什么）

| 用例 | 描述 | 状态 | 备注 |
|------|------|------|------|
| SSH-X-02 | 无 SFTP 拖拽可视化 | 通过 | 无对应实现 |
| SSH-X-03 | 无服务器分组树（平铺列表 + 搜索） | 通过 | |
| SSH-X-04 | 无 i18n（仅中文） | 通过 | |
| SSH-X-05 | 无终端录制/回放、无隧道/端口转发 | 通过 | 契约仅预留字段 |

## 通用 UI / 状态机（跨模块）

| 用例 | 描述 | 状态 | 备注 |
|------|------|------|------|
| SSH-U-01 | 无选中服务器空态「选择或添加一台服务器」 | 通过 | |
| SSH-U-02 | 连接状态机五状态（灰/橙/绿/红/橙） | 通过 | statusDotClass/statusText 单测覆盖 |
| SSH-U-03 | 子页签切换（终端/文件/监控/服务/进程/Docker） | 通过 | |
| SSH-U-04 | 删除确认弹窗通用化（服务器删除） | 通过 | |
| SSH-U-05 | 布局：侧栏 240px + 页签工作区 | 失败 | 侧栏 180px、全局状态栏缺失，见 UI-001/UI-002 |
| SSH-U-06 | 深色模式（dark token 全覆盖） | 通过 | 代码核对通过，目测见 UI-021 |
| SSH-U-07 | 用户操作可见反馈（toast） | 通过 | 主要操作均有 toast |

## 非功能需求（需求 §6）

| 用例 | 描述 | 状态 | 备注 |
|------|------|------|------|
| SSH-N-01 | 终端输入延迟 < 50ms（本地回显） | 待执行 | 后端，联调测量 |
| SSH-N-02 | 凭证加密存储（stronghold/钥匙串） | 待执行 | 后端 |
| SSH-N-03 | 终端数据不落盘、断开自动清理会话 | 待执行 | 后端 |
| SSH-N-04 | 断线自动检测 + 重连提示 | 待执行 | 后端 + `ssh://connection-status` |
| SSH-N-05 | 会话异常退出 toast 反馈 | 待执行 | 后端 |
| SSH-N-06 | 契约与 serde 逐字段对应（camelCase） | 待执行 | 阶段 B 代码核对 |
| SSH-N-07 | Rust 注释覆盖率 100% | 待执行 | scripts/check_docs.py 复查 |
| SSH-N-08 | 文件上传进度可见 | 待执行 | `ssh://transfer-progress`，联调 |

## 汇总

| 状态 | 数量 | 说明 |
|------|------|------|
| 通过 | 44 | 阶段 A 前端已验证（含单测） |
| 失败 | 14 | UI 走查缺陷（编号 UI-001 ~ UI-014，详见 UI走查清单.md） |
| 待执行 | 24 | 依赖后端/联调（阶段 B） |
| 不适用 | 0 | — |
| **合计** | **82** | 覆盖需求 P0×33 / P1×23 / P2×7 / 边界×4 / 通用×7 / 非功能×8 |
