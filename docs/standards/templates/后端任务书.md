# 后端实施提纲

> 本地任务模板：生成文件放 `.work/tasks/`，不提交 Git；长期有效要求另行提炼到现行规范。

> 默认写进已有[方案](实现方案.md)，不另建后端任务书。

按任务需要核对：真实命令与消费方、DTO 和 wire args、错误与完成事件、取消及资源所有者、数据兼容、平台条件。复用现有装配、路径和迁移入口，不按模板多造层或文件。验证边界见[20](../20-验证矩阵.md)。
