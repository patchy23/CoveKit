# 后端任务书模板

> 提交信息按[21-Git提交规范](../21-Git提交规范.md)，描述实际行为变化，不用任务编号代替内容。

> 已有总任务书覆盖时直接沿用，无需再建后端任务书。
> 复制到 `docs/batches/<批次号>/后端任务书.md`，填占位符；模板不是独立授权。
> 必读AGENTS、TODO、[模块规则](../03-模块开发规则.md)、[Rust规范](../05-Rust代码规范.md)和已确认方案。

## 输入与边界

- 项目/批次/起始HEAD：<填写>
- 用户目标与本轮不做项：<填写>
- 权威契约：`src/plugins/<id>/contracts/`（单文件模块可用contracts.ts），同步Rust models。
- 文件归属：<可修改目录与共享文件集成负责人>；同一执行者负责集成时允许按方案改装配。
- 已确认依赖、数据格式、密钥策略与平台条件：<填写，不复制历史技术示例当现行方案>

## 实施

实现<命令/事件/业务能力>，保留已发布行为与数据兼容。
命令由patchybox_module声明，路由通过patchybox_routes；不手写第二套注册表。
列清请求字段、错误、完成事件、取消标识、任务/会话释放和敏感数据责任。
按业务能力组织mod/models/子模块，不按行数机械拆分。

## 验收

命令按[验证矩阵](../20-验证矩阵.md)选择；开发负责相关自测，结果记已有方案即可。
场景：<状态机、并发、错误、持久化恢复、真实协议及平台项>。
Rust注释检查有明确覆盖范围，不宣称全语义覆盖。
新能力实际接线，旧实现清理、平台缺项与回退记录齐全。

## 交付

实现提交、消费方、测试与实际结果、装配变化、数据兼容、未完成项。
只提交自己的文件、不push；阶段与交接时更新已有方案，不强制独立记录提交。
