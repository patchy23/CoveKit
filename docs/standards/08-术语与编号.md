# 08 · 术语与编号体系

> **常青文档**。收录跨文档反复出现的缩写、编号与专有名词，供新会话和人快速对齐口径。
> **维护规则**：仅维护跨任务仍使用的术语。历史编号帮助追溯，不要求为新任务发明编号、建表或补登记。

## 1. 文档编号体系

### 1.1 批次号

任务性文档的目录名格式：`<领域>-<YYYYMM>-<编号>-<中文简述>`，例如 `ssh-202609-001-ssh工具`。

- **领域**：模块或能力名小写英文 —— `ssh` / `frp` / `db` / `arch` / `rel` / `sync` / `editor` / `storage` / `vault`
- **年月**：`YYYYMM`，取批次主导时间
- **编号**：三位序号，同一领域同一月份内递增
- **中文简述**：10 字以内

批次用于一个需持续接续的目标，不与消息、会话或提交一一对应；默认一份方案，只有必要时拆专题。目录格式是已有惯例，不是小修改必须立项的要求。
跨目录、跨批次的关联靠批次号识别，不重复建目录。

### 1.2 框架级条目编号

| 前缀 | 所属批次 | 含义 |
| --- | --- | --- |
| `AR01`–`AR07` | `arch-202609-001` | 架构重构与规范调整的 7 个事项（见下表） |
| `B1`–`B5` | `arch-202609-001` | AR 事项的合批执行顺序（B1 检查器 / B2 AR03 / B3 AR04 / B4 AR05 / B5 AR06 与 L 子项共用） |
| `D1`–`D6` | `arch-202609-001` | 剩余项决策书的 6 项决策（D1 行为测试方案 / D2、D5 串行实施与 P3 边界 / D3 测试环境与合入边界 / D4 公共数据能力依赖顺序 / D6 P7 分段与遗漏验收收口） |
| `P1`–`P7` | `arch-202609-001` | 剩余项逐项执行阶段；`P3a`/`P3b`/`P3c` 是 P3 的三个串行步骤，`P7a` 是 P7 的平台基线分段 |
| `T01`–`T16` | `rel-202609-001` | 框架可靠性与扩展治理的必修任务（T01 固定存储根 / T02 迁移预检与一致性校验 / T03 旧布局可恢复迁移 / T04 系统密钥库接入与降级透明化 / T05 合并凭证安全原语 / T06 PluginDb 迁移事务化 / T07 设置读写与 schema 统一 / T08 双平台装配与发布门禁 / T09 插件自报凭证引用 / T10 工作区生命周期与资源释放 / T11 统一长任务观察与诊断 / T12 注册表与 IPC 边界可验证 / T13 去事实源冗余与结构治理 / T14 受控「发送到」意图（可选）/ T15 本地数据导入导出（近期必做）/ T16 见该批次任务书） |
| `L0`–`L5` | `sync-202609-001` | 本地数据导入导出与云同步预留的交付阶段；**L0 是数据边界冻结**（空间/设备字段归属、稳定身份、兼容布局、generation 激活、密钥），是唯一落点，不另建 L0 任务书 |

### 1.3 AR01–AR07 对照

| 编号 | 事项 |
| --- | --- |
| AR01 | 修改生效规范与文档事实源（AGENTS.md、docs 规范、两个工程 skill） |
| AR02 | 重做可靠的规范与依赖检查（Rust 测试工具 + syn 2 AST，替代旧正则检查） |
| AR03 | 解除基础 UI 与应用服务耦合（`CredentialPicker` 等下沉 `core/vault/ui/`） |
| AR04 | SSH 状态所有权重构（60+ 平铺文件按 8 个能力域归位） |
| AR05 | Database 前端及驱动职责重构（前端四域 + Rust 驱动拆分） |
| AR06 | 固定数据上下文并接统一生命周期（`DataContext` 与 L 子项共用工作包） |
| AR07 | 统一 HTTP owner 与编译期装配（`api` 迁入 `http_ws/persistence/`） |

## 2. 架构专有名词

| 术语 | 含义 |
| --- | --- |
| **owner** | 内置功能模块的边界单位，以业务能力划分；前后端目录不要求一一对应。数据文件经 paths 解析到当前空间 data 目录 |
| **DataContext** | Rust 侧不可变上下文 `{ space_id, location, epoch }`，存储位置与数据空间的唯一事实源 |
| **spaceId** | 数据空间标识（独立数据集）；未启用多空间时为默认空间 |
| **epoch** | 本次启动的上下文标识，用于判定进程内状态的新旧 |
| **location** | 固定的设备根、空间根及 data/vault/logs/cache 实际路径；不再有旧布局枚举 |
| **maintenance_guard** | `framework/context.rs` 的互斥守卫，根迁移 / 导入提交 / 空间激活 / 更新安装共用 |
| **PluginDb** | 模块数据库统一骨架：连接生命周期 + 锁 + `PRAGMA user_version` 顺序迁移 |
| **secure_store** | 框架级安全原语（加解密、主密钥来源、原子写与备份恢复、文件权限） |
| **`patchybox_module!`** | 模块门面声明的宏：owner + featureId + `命令路径 => 中文说明`，同时生成入库元数据与 handler |
| **`patchybox_routes!`** | `plugins/mod.rs` 的路由装配宏：一行/模块生成路由、装配顺序与 `validate_routing()` 启动校验 |
| **`Ui*`** | `@/core/ui` 的公共基础控件统一前缀（`UiInput` / `UiSelect` / `UiCombobox` / `UiListRow` / `UiTabsOverflow` …） |
| **`UiCodeEditor`** | 统一的代码编辑器组件（CodeMirror 6 基座），禁止自研 textarea 编辑器 |

## 3. 检查器与门禁

| 入口 | 作用 |
| --- | --- |
| `python scripts/check_rust_rules.py` | 调用 Cargo 的 Rust AST 规范检查，含 SAFETY 逐处校验；执行边界见20 |
| `python scripts/check_docs.py` | 调用 Cargo 的 Rust 注释覆盖扫描；Markdown 链接由 check_markdown.py 检查 |
| `framework/manifest_contract_tests.rs` | IPC 清单契约：源码扫描 + 已发布命令表，拦截手写绕过 |
| `pnpm lint` / `pnpm test` / `pnpm build` | 前端门禁 |
| `cargo fmt` / `cargo clippy -- -D warnings` | CI Rust 格式和 lint 门禁；具体命令只见20 |

历史批次编号的详细定义留在对应方案，不为解释旧编号新增开发任务。
