# 09 · 平台能力矩阵

> **常青文档**。项目要求「产品能力必须覆盖 Windows 与 macOS，unsupported 必须可见」（AGENTS.md 已定技术决策）。
> 本文是那张表：每个能力在两端的真实状态与依据。**空白不等于通过**——没实测的就写「未实测」，禁止用推测填成「可用」。

## 状态口径

| 状态 | 含义 |
| --- | --- |
| ✅ 已实测 | 有可复核的实测记录（命令 / 截图 / 日志路径） |
| 🔶 部分 | 主路径可用，但存在已知差异或未覆盖分支 |
| ⬜ 未实测 | 尚未在真机验证过 |
| ⛔ unsupported | 该平台明确不支持，需要界面可见的降级或禁用 |

## 1. 框架能力

| 能力 | Windows | macOS | 依据 / 备注 |
| --- | --- | --- | --- |
| 应用冷启动（dev） | ✅ | ⬜ | Windows 见 `batches/arch-202609-001-架构重构与规范调整/P7a平台启动基线.md` |
| 应用冷启动（release） | ✅ | ⬜ | 同上；macOS 无平台环境 |
| 主窗口标题与品牌 | ✅ | ⬜ | 窗口标题 CoveKit（进程名仍 `patchybox.exe`） |
| 单实例 | ✅ | ⬜ | 次要机制窗口 `com.patchy23.patchybox-siw` 为插件机制窗口，非缺陷 |
| 系统密钥库（keyring） | ⬜ | ⬜ | T04 范围；Windows 缺 feature 的修复见 `rel-202609-001` |
| 存储根迁移与四分区布局 | ⬜ | ⬜ | T01–T03 范围 |
| 托盘显示 / 隐藏 | ⬜ | ⬜ | T08 验收项含两端 |
| 全局快捷键 | ⬜ | ⬜ | 平台冲突需分别实测 |
| 自动更新与签名 | ⬜ | ⬜ | 仅验证过 Windows 本地构建产物 |
| 拖拽（OS 文件进窗口） | ⬜ | ⬜ | 走 Tauri `dragDropEnabled`；应用内拖拽另用 pointer 自实现 |
| 资产访问 scope（TTS 等） | ⬜ | ⬜ | T08 范围：scope 跟随 `activeRoot` |

## 2. 工具能力

| 工具 | Windows | macOS | 备注 |
| --- | --- | --- | --- |
| 格式转换 | ⬜ | ⬜ | |
| 接口调试 | ⬜ | ⬜ | |
| 数据库（9 类型） | 🔶 | ⬜ | SQLite 有真实存储用例；MySQL/PG/Redis 与国产库驱动路径未全测 |
| Hosts 编辑 | ⬜ | ⬜ | 需系统权限，两端路径与提权方式不同 |
| DNS 解析 | ⬜ | ⬜ | 腾讯云 DNSPod API，本身跨平台 |
| SSH 远程管理 | 🔶 | ⬜ | 45 项真机协议冒烟由用户自测；macOS 平台验收裁定不追求补齐 |
| 文字转语音 | ⬜ | ⬜ | 依赖系统 TTS，两端实现不同 |
| FRP 配置管理 | ⬜ | ⬜ | frpc 二进制由用户自备或工具内下载 |
| 组件实验室 | ⬜ | ⬜ | 纯前端 |

## 3. 已知平台差异的处理原则

1. **接口契约两端一致**：平台差异不体现在 IPC 契约上，只在实现层分派。
2. **unsupported 必须可见**：不支持的能力要在界面上显式禁用或给出说明，不得静默失败。
3. **不靠移除平台目标规避问题**：Windows 专属调用必须加条件编译或非 Windows 明确 no-op。
4. **平台专用依赖**：新增时按 `standards/05-Rust代码规范.md` 评审并记录 target 条件。

## 4. 长期环境缺项

- **macOS 构建与冷启动**：当前无 macOS 环境，已由用户裁定不追求补齐。相关结论保持**空白**表述，**不得改写为「已通过」**。

## 5. 待补

- 逐项回填各能力的实测依据（命令、日志路径、日期）。
- T08 完成后统一复测托盘、快捷键、密钥库、更新四条。
