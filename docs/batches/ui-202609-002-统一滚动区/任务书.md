# 统一滚动区任务书

用户授权设计横向和纵向公共滚动组件，并迁移已有滚动条。

- 新增 `UiScrollArea`，支持纵向、横向和双向滚动；统一滑块、轨道、交汇角落和悬停/拖动态。
- 使用既有设计 tokens，支持随应用主题和强制深浅主题；SSH 终端保持深色滚动条。
- 全仓现有滚动容器接入；终端、编辑器等内部滚动区接入统一样式，保留其原生滚动控制。
- 保持原有 DOM、ref、事件、滚动位置读写、自动跟随、表格布局与键盘操作；不引入自绘滑块的尺寸与拖拽状态机。

实现与验证状态见[实现方案](实现方案.md)。
