# SSH 工具 · 测试任务书（agent3）

> （2026-09-12 路径说明）本文件写作时契约是单文件 `src/plugins/ssh/contracts.ts`，该文件已于 2026-09 拆为 `src/plugins/ssh/contracts/` 目录（conn/file/terminal/tunnel/monitor 等，见《2026-09-06-整改任务书》§3）。下文保留当时的写法，未逐条改写。

## 角色
你是资深 QA 工程师，按**需求文档和 UI 设计**（不是按实现）测试 patchyBox 的 SSH 远程管理插件。

## 项目背景
- 项目路径：G:\workspace\patchyBox（git-bash 环境）
- **开工前必读**（按顺序）：
  1. `docs/plugins/ssh/需求.md` —— **测试依据①（功能清单 P0/P1/P2 + 边界）**
  2. `docs/plugins/ssh/界面设计.md` —— **测试依据②（布局/交互/状态机/组件）**
  3. `src/plugins/ssh/contracts.ts` —— 契约（含事件通道 sshEvents）
  4. `docs/standards/03-模块开发规则.md` §5 质量门槛
  5. 前端代码 `src/plugins/ssh/`（当前 mock 数据模式，UI 可测）
- 后端开发并行进行中（feature/ssh-backend 分支），**后端完成前先测前端与写用例**

## 任务目标
产出可执行测试 + 需求覆盖矩阵 + 测试报告，验证 SSH 工具符合需求与 UI 设计。

## 测试依据（按此组织，不是按代码）
- 需求.md 的 P0（服务器连接管理/SSH 终端/文件管理/凭证管理）、P1（远程编辑/监控/服务/进程）、P2（Docker 等）
- `docs/plugins/ssh/界面设计.md` 的连接状态机（未连接/连接中/已连接/断开/重连）、8 模块交互、组件复用
- 边界清单（不做什么——不测试 SFTP 拖拽等）

## 工作安排（两阶段）
### 阶段 A（后端完成前，先做）
1. **测试计划**：`docs/batches/ssh-202609-001-ssh工具/测试计划.md` —— 测试范围、环境（本地 SSH 服务器可用性评估）、用例分级（P0 必测/P1 应测/P2 可选）
2. **需求覆盖矩阵**：`docs/batches/ssh-202609-001-ssh工具/需求覆盖矩阵.md` —— 每条需求 → 用例编号 → 状态（待执行/通过/失败）
3. **前端纯函数单测**：为 `src/plugins/ssh/connection/useSsh.ts` 的纯函数写 vitest 用例（`useSsh.test.ts`，放 src/plugins/ssh/，用现有测试基建跑 `pnpm test`）
4. **UI 走查清单**：`docs/batches/ssh-202609-001-ssh工具/UI走查清单.md` —— 按 `docs/plugins/ssh/界面设计.md` 逐项核对前端实现（布局/交互/空态/深色模式/反馈），当前 mock 模式下先记录"待后端接入项"
### 阶段 B（后端完成后）
5. **后端命令测试**：读 feature/ssh-backend 分支代码，验证命令与契约一致（字段名 camelCase、事件名、错误结构）；能本地跑的 Rust 单测补充（经 cargo test）
6. **联调验收清单**：`docs/plugins/ssh/联调验收清单.md` —— 有真实 SSH 服务器时的手工用例（连接/终端/文件/监控/服务/进程/docker 各一组）

## 输出物
- `docs/batches/ssh-202609-001-ssh工具/测试计划.md`、`需求覆盖矩阵.md`、`UI走查清单.md`、`联调验收清单.md`
- `src/plugins/ssh/connection/useSsh.test.ts`（可跑、通过）
- 阶段 B 后：`docs/plugins/ssh/测试报告.md`（覆盖矩阵结果 + 问题列表：编号/严重级/描述/复现步骤/截图证据）

## 约束
- **不修改实现代码**（除新增自己的测试文件）；发现问题写进问题列表（放测试报告），不自行修复
- 只新增以上文件；分支 `feature/ssh-qa`（基于 main 创建，不合并）
- 测试文件也要过 lint（`pnpm lint`）+ 全绿才能提交
- 提交信息 conventional commits 带 `ssh` 作用域；禁止 git add -A

## 最后一步
阶段 A 完成时输出总结：测试计划要点、覆盖矩阵规模、前端单测结果、UI 走查发现的问题清单（编号列表）。
