# 架构重构任务书 · 剩余项执行计划（2026-09-12）

> 2026-09-13执行更新：[剩余项决策书](剩余项决策书.md)已确定测试手法、串行执行、P3编号边界、数据前置顺序与P7分段验收。本文旧待确认、默认动作和进度仅作历史记录；冲突以新决策为准。

> 依据：`docs/batches/arch-202609-001-架构重构与规范调整/架构重构与工程规范调整任务书.md`（AR01–AR07）。
> 用户 2026-09-12 指示：复杂任务先落逐项计划书，再一项一项完成，一项一提交一验收。
> 本计划只覆盖该任务书**尚未完成**的部分，以及执行中发现的既有偏差；框架可靠性（T01–T15）与本地数据导入导出（L0–L5）以各自任务书为准，这里只把它们排成前置。

## 1. 已完成基线（不再改动）

| 批次            | 提交                            | 内容                                                              |
| --------------- | ------------------------------- | ----------------------------------------------------------------- |
| AR01            | `544b5c8` `32abec1` `28e124d`   | 规范切换：owner 边界、规模改评审信号、锁与 clone 按成本           |
| AR02            | `734593f` `12d0b3c` `e5274b2`   | 规范检查器重做（syn AST + 棘轮基线）与前端依赖守卫                |
| AR03            | `1e5277e`                       | 基础 UI 与服务解耦（凭证 UI 迁 `core/vault/ui`、剪贴板/窗口下沉） |
| AR04（目录归位） | `69f7c90`                      | SSH 60+ 平铺文件按 8 个能力域归位；**状态拆分未做**               |
| AR05（Rust 驱动） | `ab5e05b`                     | `drivers/mod.rs` 726 行拆四文件；**前端分工未做**                 |
| AR06（机制）     | `4819d67`                      | `context.rs` + `lifecycle.rs` 机制与接入；**§9.3 四项未完成**     |
| AR07            | `4819d67`                       | 静态模块清单宏、路由/装配生成、契约测试；顺带修 11 条命令未入库   |
| 构建修复        | `cdd98b6`                       | 测试二进制与主程序补 comctl32 v6 清单依赖，修 `0xC0000139`        |

门禁基线（此后每项完成都不得低于这条线）：
`cargo test --locked --no-default-features --lib` 177 passed、`--test source_rules` 29 passed；
`cargo clippy --all-targets -- -D warnings` 无告警；`pnpm test` 320 passed（37 files）、`pnpm lint`、`pnpm build`、`pnpm format:check`、`node scripts/check_frontend_deps.mjs` 全 0；`scripts/check_docs.py`、`scripts/check_rust_rules.py` 通过。

## 2. 逐项计划

### P1 · AR05 前端 `useDatabase.ts` 拆四域 + 兼容门面

- 范围：`src/plugins/database/useDatabase.ts`（1416 行）→ `connection/useDatabaseConnections.ts`（连接定义、连接/断开、当前选择与请求 epoch）、`workspace/useQueryWorkspace.ts`（tabs 与每 tab 的 SQL/dirty/result/request、运行取消、分页投影）、`catalog/useDatabaseCatalog.ts`（对象树缓存与加载状态，缓存 key 含 connection 与上下文）、`library/useQueryLibrary.ts`（历史/收藏读写）；根文件退化为门面（再导出 + 应用级协调），13+ 消费组件不改 import。
- 前置：先补行为特征测试（查询 tabs、连接切换、取消、结果分页、缓存不串连接），再做搬移。
- 验收：`pnpm test` 不低于 320 且新增用例覆盖四个域边界；lint/build/format:check/check:deps 全 0；根文件不再持有各域重复 ref。
- 状态：**已完成**（提交 `5edcb4a`；四项验收下的实测：返回面 77 key 与拆分前逐一相等、14 处消费方 import 名全部由门面再导出、`pnpm test` 37 files/320 passed 且测试文件零改动、lint/build(vue-tsc)/format:check/check:deps 全绿）。四域实现为纯搬移，域间只传只读视图与端口函数。
- 未做与理由：未新增域级特征测试。仓库现有前端测试只覆盖纯函数（无 `vi.mock` IPC 先例），域级用例需先引入 IPC mock 模式，属新增测试范式，待单独确认后再做。
- 风险：任务书 §8.2 要求「不能用旧副本覆盖新修复」，旧实现里的迁移失败/部分凭证失败提示必须保住。

### P2 · 既有文档漂移清理（独立小项）

- 范围：`docs/plugins/ssh/` 中引用的 `useTerminalLineCopy.ts`、`useTerminalSearch.test.ts` 在全仓不存在（设计稿遗留的计划文件名）；文档里 `contracts.ts` 写法与实际 `contracts/` 目录不一致。
- 验收：文档引用与仓库实际文件一致；不改任何代码与行为。
- 状态：**已完成**（提交 `55af37e` 补测试、`2b3c5cb` 文档）：补写 `terminal/useTerminalSearch.test.ts`（10 用例，前端测试 37 files/320 → 38 files/330）；`docs/batches/ssh-202609-003-ssh工具扩展/扩展任务书.md` 增「落地核对」小节、状态行改为「代码已全部落地」并指明未复测的真机验收项；四份旧任务书补 `contracts.ts` → `contracts/` 的路径说明。原清单勾选未改写（保留计划记录）。

### P3 · AR04 SSH 状态所有权拆分（拆三小步）

- 开工前盘点已完成（提交 `b247dc9`）：`docs/batches/ssh-202609-005-ssh状态所有权拆分/AR04状态拆分方案与行为清单.md` 记录同一职责存在三份实现（活跃根文件 723 行、两个无消费方的旧模块 `profiles/useSshProfiles.ts` 161 行 / `workspace/useSshIdleWatch.ts` 155 行）、四域目标分区、根的较新逻辑清单与三段计划。**待用户确认测试手法与执行者后开工**。

- P3a 行为特征测试先行：连接与恢复、主机密钥队列、终端会话生命周期、分组与凭证选择的现有行为固化为用例。
- P3b 拆分并接入：`useSshProfiles` / `useSshConnections` / `useSshLifecycle` / `useHostKeyQueue` 四个所有者模块；profiles/groups 只创建一次，连接操作走窄接口取配置快照。
- P3c 删除旧实现与重复状态（旧 localStorage 转换只留一份实现）。
- 验收：实际消费方改用新模块；删除/移动分组反馈一致；取消握手不留队列；旧数据夹具结果与基线一致。
- 风险：任务书 §7.2 的 5 条必须保住的行为逐条对照（迁移失败提示、部分凭证失败、主机 key 队列并发确认、异步结果不得重开页签）。

### P3.5 · AR05 Database 四域行为网与驱动矩阵（决策书 §2.3）

- 已完成（2026-09-13）：四域 + 兼容门面行为网 28 用例 / 6 组（`src/plugins/database/useDatabase.behavior.test.ts`），覆盖决策书 §2.3 的七个必需场景。首轮复现 5 处失败 —— 结果按「当前活动页签」回填、取消按连接一刀切 —— 按最小请求身份修复：前端 `requestId.ts` 生成请求身份，`dbc_execute` / `dbc_cancel` 契约携带它，后端取消句柄改按请求身份登记（同连接多页签互不牵连），任务书 §7.2「异步结果不得改变已切换状态」由该网锁定。
- 已实测：SQLite 临时文件库真实存储 5 用例（写入后重开同文件仍可读、NULL/二进制字符串化、超限标记截断、错误 SQL 不伪装成功），见 `src-tauri/src/plugins/database/drivers/sqlite.rs`。
- 环境待补：MySQL / PostgreSQL / Redis（本机无服务、无 docker、无客户端）、Oracle / 达梦 / 金仓 / Vastbase / PolarDB（需远端 agent 与实例），以及应用内真库冒烟。补齐步骤与结果占位见 `docs/batches/db-202609-001-database四域重构/AR05行为网与驱动矩阵.md`。
- 验收：行为网全绿 + 驱动矩阵逐驱动标注已测/未测 + 缺项给可执行步骤；不以结构提交代替总验收。

### P3.6 · AR03 补证据（2026-09-13 完成）
- 已完成：基础 UI 无应用服务挂载用例（`src/core/ui/components.test.ts` 新增 2 例：真挂 `UiModal`/`UiTree`/`UiDataGrid`/`UiSplitPane`/`UiCombobox` 并断言无注入缺失告警 + 源码依赖守卫）、凭证选择与新建失败行为用例（`src/core/vault/ui/credentialComposite.test.ts` 9 例）、复制反馈三态用例（`src/core/feedback/useCopy.test.ts` 6 例）。
- 手法要点：弹窗经 reka 传送门渲染到 `document.body`，必须 `attachTo: document.body` 后按 body 查询；`stubs: { teleport: true }` 会替换 reka 自己的 `Teleport` 组件导致内容整体消失（曾误判为组件缺陷）。
- 未覆盖：`UiSelect`/`UiCombobox` 下拉展开与选项点击（依赖指针事件，稳定性不足，归 UI 走查）；不要求全量控件机械加测。
- 记录：`docs/batches/arch-202609-001-架构重构与规范调整/AR03补证据记录.md`。

### P4–P6 · AR06 的外部前置（按各自任务书执行，本计划只排期）

- P4 = 可靠性 A 批（T04/T05：系统密钥库真实接入、旧密钥兼容、凭证崩溃恢复）→ 解锁 AR06 ③（key 与凭证上下文）。
- P5 = 可靠性 D 批（T10/T11：工作区生命周期与诊断）→ 解锁 AR06 ②（前端页签「未保存则拒绝关闭」）。
- P6 = E 批中的 L0/L1（路径与设置边界冻结、数据空间与字段路由）→ 解锁 AR06 ①（`spaceId` 由承载位转为真实身份）。**L1 已落地（`2db2dba`，状态 🔶 见台账）→ AR06 ① 已解锁**；AR06 的④之外各项均已接入，W4（空间字段路由）随后由 `1b4be9a` 补齐。

### P7 · AR06 收口验收（2026-09-16 完成，Windows）

- 内容：P4–P6 接入后补 AR06 ④（双平台冷启动与旧数据夹具），按任务书 §9.3 判定完成，仅机制与接口不算完成。**现状：P4/P5/P6 前置均已解除，AR06 ①②③ 与 W4 已接入；本项只剩验收**。
- 验收：Windows + macOS 冷启动无 panic；旧数据夹具（旧布局、旧密钥、半迁移）通过；页签有未保存内容时关闭被拒绝且原因可见。macOS 侧按用户 2026-09-13 裁决登记为 ⏸、不作为收口条件。
- 结果（Windows）：两次 `pnpm tauri dev` 新构建冷启动无 panic、无装配与 IPC 报错；旧布局只读核对通过（只写 `settings.json`、`preferences.json`、`data/frp.db`，旧密文/主密钥/SSH 凭证/旧库与旧 `patchybox.json` 均未被改写）；页签关闭被拒且原因可见，「取消」保留页签与 frpc 进程、关闭后进程消失。走查期间发现并修复一处真机缺陷——关闭协商的 Tauri 参数名与前端契约不匹配，导致「被拒绝时显示原因」此前在真机上从未成立（`8f110d1`）。剩余两条观察（同一工具两条生命周期条目、前后端拦截口径不同源）记在方案 §8.4，未改。
- 记录：[AR06实现方案](AR06实现方案.md) §8.2–§8.4。

## 3. 每项的执行纪律

1. 一次只做一项；做完立刻跑该风险层门禁并单独提交，只 `git add` 本项文件，不 `git add -A`。
2. 验证命令的唯一来源是 [20-验证矩阵](../../standards/20-验证矩阵.md)：按变更定档后只跑本档列出的行。本地只做静态与格式检查（前端 `pnpm run check` + `scripts/` 下的 Python 静态检查），编译与测试进 CI 或由用户手动执行；本地绿灯不得写成「已通过」。动持久化/凭证/注册 → 加旧数据夹具与故障恢复用例。
3. 改 `tauri.conf.json`、插件注册、`Cargo.toml`、能力权限 → 提交前冷启动冒烟（本轮实测方式：直接跑 `src-tauri/target/debug/patchybox.exe`，存活 12 秒且无 panic 输出）。
4. 每项完成即在 `TODO.md` 勾掉对应条目，并在本文件该项标注完成提交。
5. 不新增功能；行为不确定的地方先把现状固化成特征测试再动。
