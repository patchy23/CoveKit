# 单人项目的 AI 开发约定精简

来源：2026-09-17 用户明确要求检查并直接删减、补充项目约束与测试；项目由一人维护。起始 HEAD：`78f93de`。

## 目标与边界

让 agent 根据当前代码、用户目标和必要约束准确完成开发。精简的是重复流程、过时事实与低价值测试，不降低数据兼容、凭证保护、错误反馈、取消和资源释放要求。历史批次保留证据身份，不逐份翻新；不重写 Git 历史，不改产品功能或安装新依赖。

## 已确认的问题与处理

| 问题 | 处理与依据 |
| --- | --- |
| 公共改造固定两份文档，多个入口重复记状态 | 按跨会话接续和重要取舍决定是否需要一份方案，复杂工程按需拆分；TODO 只作链接 |
| 单人项目套用团队角色、强制隔离与交接包 | 默认一个 agent 串行完成；只在明确委派时补充边界；保留用户或其它会话未提交内容的保护 |
| 通用测试手册混入旧 SSH 阶段、工具 API 与本机坐标 | 删除现场流水，保留可移植的方法、真实行为证据与工具能力边界 |
| Rust 检查被误称为无 Cargo 静态扫描 | 两个 Python 入口实际调用 Cargo；从默认本地门禁移除，CI 保留 AST 规则及夹具 |
| 提交钩子按空格拆文件名，检查工作树冒充暂存内容 | 使用 Git NUL 路径列表；相关文件部分暂存时明确拒绝误验；删除文档时检查残留引用 |
| 临时目录虽被 Git 忽略仍进入 ESLint | 统一排除 .work、覆盖率与测试报告；可复用测试留仓库，临时输出留本地 |
| 相同依赖边界由正则与 AST 重复检查 | 删除旧插件边界测试和 UI 内重复扫描，保留 AST 依赖守卫与负例 |
| 初始化占位测试、纯样式类名断言、旧迁移名称守卫 | 删除不提供独立行为保障的用例；保留真实交互、IPC 载荷、取消、故障、隔离与资源回归 |
| 旧架构、术语及模块示例与源码矛盾 | 核对当前 context、paths、装配后修正文档；删除非事务迁移示例与机械模板要求 |
| 每个公开结构体字段都必须有注释，与“非显然字段需说明”冲突 | 字段缺注释改为提示，模块和 API 职责仍检查；保留单位、空值、兼容与凭证语义要求 |

本地测试裁决已更新：用户于本次会话明确允许相关轻量单测，完整构建、Cargo、真机及联网测试仍交用户或 CI。Git 忽略不影响可复用测试源码入库。

## 测试取舍

- 删除 `tests/sanity.test.ts` 的初始化占位用例、`pluginBoundary.test.ts` 的重复正则实现、components 中重复的依赖扫描；边界由 AST 守卫及正反例覆盖，并补齐 pinia/平台/反馈层与空扫描失败检测。
- 删除只判断颜色类、搜索样式类、managed 无内联 style 及旧编辑器名称的用例。保留尺寸/字体的公开映射契约、as-child 元素和事件保持、真实控件操作与当前编辑器边界。
- 修正公共按钮和弹窗测试的旧选择器，统一自动卸载挂载组件；未把失败用例直接删掉。滚动入口守卫改为解析模板 class，并补说明文字不误报的负例。
- 保留 IPC wire args、业务纯函数、关闭策略、乱序/取消/故障/存储回归和可复用真实服务测试。不同 owner 的相似测试有不同策略，不按代码形似批量删除。Rust 旧检查器对照中仍有特定 cfg/属性回归，未仅按文件名视为垃圾。

审查覆盖项目入口、现行规范、模板、Git/CI、测试发现与忽略规则；测试内容重点核对公共 UI、依赖守卫和代表性的业务/生命周期/存储用例。没有把静态分类冒充逐条全仓测试或安全审计。

## 验证与状态

实施已完成。Python 工程检查器 51 项隔离测试通过；本次相关前端 5 个文件共 46 项测试通过。前端 lint/格式、依赖检查、全仓 Markdown 链接与入口预算通过；Rust 检查器修改仅以独立 rustfmt 做语法/格式检查，新增字段提示回归待 CI 执行。

未运行 Cargo、产品构建、真机和联网测试，也未推送或宣称远端 CI 已通过。历史批次保持原有证据；本次不创建逐条人工验收任务。
