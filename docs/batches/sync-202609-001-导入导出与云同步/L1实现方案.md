# sync-202609-001 · L1「数据空间（namespace）骨架」实现方案

> 档位：**S3**（碰持久化、密钥、迁移、启动路径）。按 [ADR-0007](../../adr/ADR-0007-验收按变更分档与证据等级.md) 与 [20-验证矩阵](../../standards/20-验证矩阵.md) 选行，不兜底跑全量。
> 本方案不改变 [L0 数据边界冻结条款](导入导出与云同步预留任务书.md)（§13）的任一条；只把其中「空间」这一层在本批落成骨架。

## 1. 范围口径（2026-09-15 用户裁决）

**空间 = namespace。** 一套空间的资料是完整独立的一份：数据、凭证、偏好、密钥都在里面。切到另一个空间，理论上就是一个全新的环境。

本批（L1）只做三件事：

1. 把「空间」从一句话落成代码里的**唯一解析来源**——凡是取数据位置、取凭证、取偏好、取密钥，都必须经过它；
2. 把**全量隔离**做实：路径、密钥、偏好都按空间分开，而不是只分开目录；
3. **只放置一个初始的默认空间**，其余一律不管。

因此本批**产品面零变化**：用户看到的还是现在这个应用。变的是数据位置不再硬编码，而是由空间解析决定——这是后面能加第二个空间的前提。

**明确不在本批**：新建 / 切换 / 重命名 / 删除空间，空间索引与待激活状态，空间界面与管理入口，记录级稳定标识（`entityUid` / `revision`）。多空间的管理要跟「用户管理」一起设计，不是加个界面就完事。

> 与任务书 §10 分批表的差异：L1 行原文含「稳定 UID 与修订入口」。按本次裁决，该部分**本批不做**，列名与迁移方式待 L2/L3 定义合并语义时再定；本批只在 §13.2 既有约束上保留「空间 id 不得进入记录身份」。该行留待 L2 开工时一并更新。

## 2. 现状核对（源码事实）

| 位置 | 现状 | 对 L1 的影响 |
| --- | --- | --- |
| `framework/context.rs:54` | `DataContext` 已在，但 `space_id()`（`:56`、`:67`）返回 `&'static str`，值就是常量 `DEFAULT_SPACE_ID` | 空间标识目前是编译期字面量，不是真实来源，必须变成解析出来的值 |
| `framework/storage/mod.rs:108` | `make_location(root)` 是**唯一**位置构造点，形态固定为「根 + `data`/`vault`/`logs`/`cache` 四分区」 | 位置模型已收在一处，扩展它比散改便宜；但只有一种形态，表达不了分区空间 |
| `framework/paths.rs:128` | 分区路径全部由 `storage_root(app)?.join(..)` 现算 | 改成读 `location` 字段即可让**所有**消费方获得空间隔离 |
| `framework/secure_store/key/mod.rs:37` | `KEYRING_SERVICE = "com.patchy23.patchybox"` 是常量；`VAULT_KEY_SPEC`（`:92`）/ `CREDENTIALS_KEY_SPEC`（`:105`）的 `account` 是 `&'static str` | 现在**无法表达每空间独立密钥**：只分目录会让两个空间共用同一把钥匙 |
| `framework/secure_store/key/resolve.rs:31` | keyring target 由 `{account}.{service}` 拼成 | 作用域换 `service` 与换 `account` 都可行；本轮按裁决换 `service` |
| `framework/vault/store/paths.rs:8`、`framework/credentials.rs:38` | 均取 `storage_root(app)` 再拼子目录，`credentials-master.key` 落在数据目录 | 两把密钥（vault 主密钥、命名空间密文主密钥）都要随空间走 |
| `framework/settings.rs:122` | `RESERVED_KEYS` 四项；设置全部落在自举配置的 `app` 对象里 | 空间级与设备级混在一个文件，切换空间会串值 |
| `framework/storage/{scan,verify,plan}.rs` | 枚举范围是四个分区目录 | 换盘迁移不会带走 `spaces/` 下的非活动空间 |
| `lib.rs:62–115` | 启动顺序：根迁移 → `context::init_from_app` → 布局迁移 → 插件 | 上下文一旦固定不能换根，空间解析必须排在最前 |
| `paths` 全部消费方 | `PluginDb`、`credentials`、`vault`、`ssh`（凭证/known_hosts/会话日志）、`frp`、`database`（密钥与 agent 缓存）、`tts` 缓存 | 都只经 `paths` 取路径，**插件侧不需要各自改**，这是本批能做到小改动面的原因 |

## 3. 不变式

1. **默认空间零迁移（红线）**：本机存在真实凭证密文与密钥。默认空间的数据位置、密钥条目、密文文件一律保持原样，任何新增计算都必须对默认空间回落到与今天逐字符相同的路径。
2. **唯一来源**：一次运行只解析一次位置，`paths` 与 `PluginDb` 消费同一实例；不得出现「某处还按根目录现拼」的第二套。
3. **全量隔离**：数据、凭证、偏好、两把密钥都按空间分开。密钥必须分开到 keyring 条目一级——只分目录等于没隔离。
4. **设备级配置是唯一共享项**：存储根、开机自启、默认下载目录、`frpc` 路径、主机级设置不随空间变化（它们是设备事实，按空间各配一份只会互相矛盾）。这是「切换即全新环境」的**明确例外**，写在文档里而不是靠记忆。
5. **越界零容忍**：空间 id 只接受小写 UUIDv4 形态，作为目录名与 keyring service 片段前必须校验，禁路径穿越与名称注入。
6. **不静默兜底**：索引或目标不可读一律可见失败，不得静默退回默认空间，也不得新建空环境冒充成功。维护互斥仍只有 `context::maintenance_guard()` 一处。

## 4. 关键决策

| # | 决策 | 理由与代价 |
| --- | --- | --- |
| D1 | 位置用**一个 descriptor 表达两种形态**：`StorageLocation` 增 `layout`（`LegacyFlat` / `Partitioned`），`data`/`vault`/`logs`/`cache` 改为由形态算出的字段 | §13.3 要求两种布局都能被同一个启动解析表达。代价：`make_location` 的调用点要区分形态，`paths` 内几处 `root.join(..)` 改读字段 |
| D2 | 密钥作用域按**service 换名**（用户裁决）：默认空间沿用 `com.patchy23.patchybox`，其他空间用 `com.patchy23.patchybox.<spaceId>`，`account` 保持 `vault-master-key` / `credentials-master-key` 不变 | §13.5 要求默认空间沿用现有条目（否则升级即解不开）。换 service 而不是换 account，是为了让「同一把钥匙在不同空间的同名条目」在系统凭据管理器里天然分组。代价：`KeySpec` 要能带作用域，`secure_store/key/{mod,resolve,status}.rs`、`credentials.rs`、`vault/store/*` 的取用点都要按当前空间取 spec |
| D3 | 当前空间由**自举配置的一个保留键**给出：`activeSpaceId`（首次启动写入 `default`），缺失或非法时回落 `default` 并登记，不静默新建 | 需要一个真实来源，否则又退回编译期常量、第二空间无从接。只加一个键，不引入空间索引与管理动作。代价：`RESERVED_KEYS` 增一项，通用设置写入要拒它 |
| D4 | 空间解析排在**根迁移维护之后、`context::init` 之前**；上下文未初始化时 `paths` 保留现有即时解析回落 | 上下文固定后不能换根。代价：启动顺序成为契约，`lib.rs` 要写明原因并加启动期守卫用例 |
| D5 | 空间 id 固定为**小写 UUIDv4 字符串**，新增 `space::is_valid_space_id()`，校验通过才允许拼目录名与 service | §13.2 冻结（禁邮箱/机器名/路径派生）。代价：新增一处校验点 |
| D6 | 本轮**不建空间目录**：默认空间继续用现有根（`LegacyFlat`）；`spaces/<id>/generations/<n>/` 形态由测试夹具构造，产品面不产出 | 只有一个空间时建目录结构纯属空转，且会动到真实数据位置。夹具构造既能证明隔离，又不留无功能占位 |
| D7 | 空间级偏好统一落 `<space>/preferences.json`，默认空间做**一次性迁移**（读旧键 → 写新文件 → 旧键删除；失败则回落读旧键，设置不丢） | §13.6 要求 settings 对外语义不变、由框架适配层完成归属分流。代价：本批唯一一处迁移，需要「旧键回落读」这条兼容路径 |
| D8 | 根迁移与日志/缓存的枚举范围改为「四分区 + `spaces/` 下已存在的空间与必要代际」；`layout.rs` 的布局迁移只在 `LegacyFlat` 下执行 | §13.3 要求迁移覆盖空间索引引用的全部空间。代价：`scan`/`verify`/`plan` 三处口径同步改 |
| D9 | 日志与缓存按空间分层（`<deviceRoot>/{logs,cache}/<spaceId>/<scope>`），默认空间的旧日志做一次幂等搬迁，失败保留原位并回落读取 | §3.2 冻结写法就是「设备根下按 spaceId/owner 分隔」。代价：本批第二处迁移，须保证失败不导致界面里的日志消失 |

## 5. 改动面（逐文件）

### 5.1 新增

| 文件 | 内容 |
| --- | --- |
| `src-tauri/src/framework/space/mod.rs` | 空间模块门面：`current()`（唯一解析入口）、`is_valid_space_id()`、`active_space_id` 读写、`SpaceId` 类型 |
| `src-tauri/src/framework/space/tests.rs` | 校验用例、解析回落用例、两空间隔离用例（用例体包在 `#[cfg(test)] mod tests` 内，见 [05-Rust代码规范](../../standards/05-Rust代码规范.md) 的独立用例文件说明） |
| `src-tauri/tests/fixtures/`（按需） | 第二空间夹具构造器：建 `spaces/<uuid>/generations/1/{data,vault}` 与 `preferences.json`，供隔离用例使用 |

### 5.2 修改（后端）

| 文件 | 改动 |
| --- | --- |
| `framework/context.rs` | `DataContext.space_id` 改为解析出的值；`init` 增加空间与代际入参；`init_from_app` 改为「自举配置 → 空间解析 → location」 |
| `framework/storage/mod.rs` | `make_location` 按 D1/D6 支持两种形态；`storage_info` 增布局字段（`spaceId` 契约里已有） |
| `framework/paths.rs` | 分区路径改读 `location` 字段；`data_path` 回落语义按布局收窄；`storage_root` 语义明确为「设备存储根」（展示与设备级产物用） |
| `framework/secure_store/key/mod.rs` | `KeySpec` 支持作用域；新增按空间取 spec 的入口；默认空间取值不变 |
| `framework/secure_store/key/resolve.rs`、`status.rs` | spec 由调用方按当前空间给出；降级密钥文件随空间目录 |
| `framework/credentials.rs` | 数据目录改由 `location.data` 给出；主密钥与命名空间密文按当前空间解析 |
| `framework/vault/store/paths.rs` | vault 目录改由 `location.vault` 给出 |
| `framework/settings.rs` | `RESERVED_KEYS` 增 `activeSpaceId`；按 §13.1 归属表实现设备级/空间级分流（空间级落 `<space>/preferences.json`），对外命令语义不变；按 `TODO.md` §五清理字段分类表里已失效的行（`globalHotkey`、`downloadMirror`） |
| `framework/storage/{scan,verify,plan}.rs` | 枚举范围按 D8 扩展 |
| `framework/storage/layout.rs` | 布局迁移只对 `LegacyFlat` 生效 |
| `framework/storage/recovery.rs` | 增「自举配置不可读」「目标空间不可读」的恢复状态变体，复用现有恢复页 |
| `framework/manifest_contract_tests.rs` | 若命令清单有变动则同步（契约测试会抓漏登记） |
| `lib.rs` | 启动顺序按 D4 调整并写明原因；激活期与维护互斥的接线 |
| `framework/mod.rs` | 新模块登记 |

### 5.3 修改（前端）

本批**不改前端**。`StorageInfo` 契约里已有 `spaceId`（`src/core/ipc/contracts.ts:267`），新增的布局字段属可选扩展，界面留到空间管理设计时一并做。

### 5.4 明确不改

`plugins/*` 各 owner 的数据库与凭证读取路径（经 `paths` 自动获得空间隔离）；`vault/mod.rs` 的业务读写；`tasks.rs` 的代际判定（AR06 已接，本批只需让代际值真实可解析）；任何导入导出能力（L2/L3）。

## 6. 实施步骤（每步一次提交，只 add 本步文件）

### W1 · 位置 descriptor 与路径空间化

- 内容：`StorageLocation` 按 D1 扩形态与字段；`paths` 分区路径改读字段；`init` 增入参；`data_path` 回落按布局收窄。
- 判据：默认空间下 `data`/`vault`/`logs`/`cache` 四个路径与改动前**逐字符相等**（对拍用例把旧算法的结果写死为期望值）；`cargo test --lib` 全绿。
- 风险：路径语义漂移会让真实数据「找不到」。对拍用例是本步硬门禁。

### W2 · 空间解析与自举

- 内容：`space` 模块（D5 校验 + D3 自举键 + `current()`）；`context::init_from_app` 改为按解析结果构造位置；`lib.rs` 顺序按 D4 调整。不建空间目录（D6）。
- 判据：`space::current()` 是唯一来源（全仓无第二处拼空间路径）；`activeSpaceId` 缺失/非法时回落 `default` 并登记，不静默新建；启动期守卫用例证明沿用默认空间的路径与今天一致。
- 风险：启动顺序错会让上下文先锁死默认空间。

### W3 · 密钥按空间

- 内容：`KeySpec` 作用域化 + 按空间取用；降级密钥文件随空间目录；`credentials.rs` / `vault/store/*` 改按当前空间解析；把 D2 的命名定稿写进本方案与台账。
- 判据（§13.5 要求两层都验）：① 默认空间密文在改动后仍可解（用真实目录的**副本**夹具，不碰真实目录）；② 空间 A 的密文用空间 B 的密钥解不开；③ 跨空间取用不命中同一 keyring 条目。
- 风险：**本步最高**（本机有真实命名空间密文）。红线：默认空间零改动；不在真实用户数据目录上做故障注入。

### W4 · 设置与偏好按归属分流

- 内容：按 §13.1 归属表分流（设备级留自举配置，空间级落 `<space>/preferences.json`）；默认空间一次性迁移 + 旧键回落读（D7）；清理分类表失效行。
- 判据：空间级键（主题、语言、工具设置、收藏、最近使用）在两空间各存一份且互不可见；设备级键（存储根、布局版本、开机自启、下载目录、`frpc` 路径）切换空间后不变；FRP 直读设置的那条路径不再跨空间串值；迁移中断时设置仍可读（回落旧键）。
- 风险：插件自行合并两份配置。对 FRP 这条已知直读路径单独加用例。

### W5 · 迁移范围、日志缓存与夹具体检

- 内容：`scan`/`verify`/`plan` 按 D8 扩范围；`layout.rs` 只对 `LegacyFlat` 生效；日志与缓存按 D9 分层并搬迁；补齐隔离夹具（第二空间）与故障注入。
- 判据：换盘迁移后**活跃空间与非活跃空间都可用**；迁移中断不产生混合库；`storage_info` 展示的空间与布局与实际目录一致；两空间同名记录互不可读。
- 风险：迁移清单口径变化最易出错，沿用 T01/T02 的「只复制不删除 + 校验通过才提交」约定。

## 7. 验证（S3 档，命令取 [20-验证矩阵](../../standards/20-验证矩阵.md)）

| 行 | 命令/动作 | 通过判据 |
| --- | --- | --- |
| Rust 格式与静态 | `cargo fmt --check`、`cargo clippy --locked --all-targets -- -D warnings` | 无 diff、零告警 |
| Rust 测试 | `cargo test --locked --no-default-features --lib`、`--test source_rules` | 全绿（含新增用例） |
| 前端 | `pnpm test`、`pnpm lint`、`pnpm build`、`pnpm check:deps` | 全绿、零告警（本批不含前端改动，仍按 S3 行执行） |
| 文档 | `check_docs.py`、`check_markdown.py`、`check_doc_budget.py`、`check_progress.py`、`check_rust_rules.py` | 全部通过 |
| 启动 | `pnpm tauri dev` 冷启动一次 + release exe 冷启动一次 | 无 panic，默认空间数据可读 |
| 数据安全 | 对真实目录做**只读**核对（文件与 keyring 条目比对），故障注入一律用临时目录夹具 | 真实密文可解、无新增/改写文件 |
| 平台 | Windows 实测；macOS 按裁决不测 | 两端差异点登记到 [09-平台能力矩阵](../../standards/09-平台能力矩阵.md) §3 |

**证据等级如实声明**：由于本批不产出第二个空间的任何产品入口，「两空间互不串读」「跨空间密钥不可解」只能由夹具与单测证明，属**单测级证据，不是真实链路**。必须这样写，不得表述为已通过真实操作验证。真实链路的多空间能力随管理入口一并验收。

## 8. 明确不做

新建 / 切换 / 重命名 / 删除空间；空间索引与待激活状态；空间界面与任何用户可见入口；`entityUid` / `revision`（含入口）；空间热切换、多空间并发运行、团队与权限；L2 包格式与选定导出/隔离导入；L3 冲突预览、引用重映射、代际提交与回滚；L4 其余 owner 的导入导出与 UID 逐列落地；L5 全量验证；云账户与任何网络同步。

## 9. 完成判据

| 交付物 | 判据 |
| --- | --- |
| 唯一空间解析 | `space::current()` 是取数据位置、凭证、偏好、密钥的唯一来源；全仓无第二套 |
| 两种布局的 descriptor | 默认空间与夹具构造的分区空间都能解析出唯一 `StorageLocation` |
| 全量隔离 | 数据、凭证、偏好、两把密钥都按空间分开；密钥分开到 keyring 条目一级 |
| 默认空间零变化 | 升级后路径逐字符相同、真实密文可解、无文件被新增或改写 |
| 设置归属分流 | 设备级与空间级分开存放，插件不自行合并；字段分类表失效行已清理 |
| 迁移与维护 | 换盘迁移覆盖非活动空间；中断不产生混合库；维护互斥仍只有一处 |
| 文档同步 | 本方案、台账、任务书差异登记齐全；证据等级如实标注 |

**完成判据一句话**：默认空间一个字节没动，而「换一个空间就是全新环境」这件事在代码里已经成立——数据、凭证、偏好、密钥四处都按空间取，且两空间互不可读有实测结论。仅机制与接口不算完成。
