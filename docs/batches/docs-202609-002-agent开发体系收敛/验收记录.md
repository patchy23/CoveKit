# Agent开发体系收敛验收记录

> 日期：2026-09-13；对照基线：e2f514e。实现提交由台账回填，不在同一提交自填hash。
> 本记录是本批证据，当前进度只见[台账](../../进度台账.md)。

## 落地结果

| 原问题 | 调整 |
| --- | --- |
| 必读遗漏裁决 | TODO进入默认集合，需求分册链接裁决，预算检查强制AGENTS/TODO不能消失 |
| 多个进度权威 | 台账唯一进度；README纯导航；工作流与16/17明确证据、裁决、状态分工 |
| 假绿与CI缺失 | 新超预算非零退出；严格表头/状态、完成证据、任务文件覆盖；独立docs-checks job |
| Markdown误跑Cargo | 新check_markdown；Rust check_docs仍兼容，仅负责Rust注释 |
| 危险清理配方 | 工作流与浏览器手册不再按进程名/裸端口强杀；只清理已确认归属实例 |
| 旧技能/模板漂移 | 工作流、16/17及三类模板改为工具中立；去掉旧组件、旧路径、手写IPC注册示例 |
| 档位与返修重叠 | 按行为/安全/持久化影响定档，多文件不自动升级；三轮无新证据转诊断，不盲修 |
| 提交hash自引用 | 实现先提交，台账独立记录提交引用已存在hash；关键词搜索不证明零实现 |

## 本地验证

- `python -m unittest discover -s scripts -p test_doc_checks.py`：22个用例通过。
- 新文档30,001字符、501行、遗漏TODO、非法状态中夹合法emoji、已完成无证据、不存在提交/文件：负例均被拒绝。
- Markdown失效文件/锚点、引用式链接缺失：负例拒绝；重复标题锚点、中文编码路径、父目录链接、示例与外链的边界用例通过。
- `check_markdown.py`、`check_progress.py`、`check_doc_budget.py`：当前仓库检查通过。
- 本次Markdown/YAML使用已安装Prettier格式化并检查；`git diff --check`通过。
- CI增加Python3.12独立文档任务和完整Git历史；两平台打包依赖它。YAML由Prettier解析，job依赖与命令人工核对；GitHub远端尚未运行，不宣称CI已经绿。

## 读取路径核对

| 场景 | 实际导航与观察 |
| --- | --- |
| 按钮文案 | 默认4份 → 11必读摘要 → 实际组件；S1不建多份任务文档，token/键盘语义要求可找到 |
| IPC修复 | 默认集合 → 02/03相关节 → HTTP的contracts.ts、ipc.ts与Rust http_ws/mod.rs → 19/20；确认存在实际入口，不假定每模块都用contracts目录 |
| SSH新能力 | 默认TODO已列SSH Config等不做项；07a顶部再链接TODO，需求规划不再被当成本轮授权 |
| 接手批次 | 默认地图 → 台账搜索arch/ssh批次 → 既有方案与证据；AR06未完成与用户不补平台验收仍区分，不重置用户裁决 |

体量按Unicode字符计，不是token：AGENTS从8,609降为2,500；默认集合从25,788降为8,405（约减少67%）；UI必读摘要391字符，文案任务文档入口共8,796字符，另按任务读取实际组件。复杂IPC任务读取相关节与代码，不强行限制到与文案任务同一预算。

## 局限与回退

- Markdown检查覆盖仓库显式内联、完整/折叠引用式本地链接与ATX标题锚点；不联网检查外链，不校验普通反引号路径/代码示例，不声称完整CommonMark或语义证明。
- 台账仅验证结构、文件/提交存在；提交是否实现该验收点仍需人工与行为测试证明。子项说明可以使用父批次证据。
- 07产品总纲保留既有20,487字符存量例外，本批没有抬高预算或新增例外；默认预算收紧到15,000字符。
- 未改业务代码或Rust规则，不启动用户应用、不跑无关应用全量测试、不改变用户已裁决不补项。
- 按本批实现及记录提交逆序回退即可；历史手册经验保留在Git，可按需查，不重新作为默认必读。
